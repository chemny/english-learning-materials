---
name: english-learning-materials
description: Create single or batch English vocabulary-review or unit-review posters from a specified textbook, edition, grade, term, and unit. Use when an AI agent needs to verify current textbook metadata, deterministically select source-backed vocabulary, sentence patterns and learning tips, lock exact poster modules in a Markdown manifest, generate a complete AI image by default, and validate text, module count and illustrations before delivery.
---

# 英语学习资料生成器

把教材核验、内容整理、用户确认、纯生图和结果验收串成一个可追溯流程。默认生成完整位图；不要自动改用 HTML、SVG、Canvas 或确定性文字排版。

## 读取规则

- 联网检索教材或单元时，读取 [references/source-policy.md](references/source-policy.md)。
- 确定资料类型、检索字段或确认卡结构时，读取 [references/content-contracts.md](references/content-contracts.md)。
- 创建或检查任务清单时，读取 [references/manifest-schema.md](references/manifest-schema.md)。
- 选择风格或编写生图提示词时，读取 [references/style-presets.md](references/style-presets.md)。
- 选择内置风格时，读取对应风格协议；锁定核心风格锚点，并根据内容负载和参考图调整精确比例、列数、人物数量与装饰道具。
- 确定学习者阶段、人物年龄或画面成熟度时，读取 [references/age-adaptation.md](references/age-adaptation.md)。
- 批量生成或同一内容生成多套主题时，读取 [references/multi-agent-batch.md](references/multi-agent-batch.md)。
- 批量任务、跨主题对照或词义较抽象时，读取 [references/illustration-semantics.md](references/illustration-semantics.md)。
- 生图前和交付前，读取 [references/qa-checklist.md](references/qa-checklist.md)。

## 核心原则

1. 把教材版本正确性和时效性置于生图速度之前；用户未指定年份时默认核验当前最新适用版本。
2. 设置两个硬确认门：先确认教材身份，再确认最终可见文字清单。
3. 只把已确认内容写进生图提示词；不得补写未经来源支持的教材事实。
4. 把 `material-manifest.md` 作为每份资料的唯一内容源。
5. 默认使用纯生图；批量任务每张图单独调用一次图片生成。
6. 不因版面拥挤擅自删词、缩写、改译或改变句义；应拆分页面并重新确认。
7. 交付前逐字检查英文、中文、句型和清单外文字。
8. 根据年级自动推断学习者年龄段；用户明确提供年龄时以用户指定为准，并同步适配人物、场景和信息密度。
9. 风格渲染按“参考图优先、文字视觉 DNA 兜底”执行：用户参考图或所选主题的内置案例图可用且运行时支持图片输入时，默认用参考图控制版式、配色、信息密度和装饰节奏；否则自动使用对应风格协议的完整文字视觉 DNA，不询问、不报错、不停止。
10. 新任务只支持两种模式：`词汇复习海报` 只能有核心词汇；`单元复习海报` 必须且只能有核心词汇、核心句型、知识提示三个主体模块。
11. 标题与单元信息属于头部，人物与装饰属于视觉元素，均不得变成学习模块。
12. 新任务使用清单版本 4；内容数量由候选池和确定性排序计算，不允许生图 Agent 随机决定。

## 工作流

### 1. 收集任务参数

确认会实质影响结果的信息：

- 资料类型：`词汇复习海报` 或 `单元复习海报`。
- 生成规模：单张或批量；批量时列出单元或资料清单。
- 教材身份：出版社、教材名称、版本或年份、年级、册次、单元编号。
- 学习者：实际年龄或学习者阶段；未提供时根据年级册次自动推断。
- 输出要求：尺寸、横竖版、每张信息量、是否包含音标/词性/中文/例句。
- 视觉风格：预设风格或用户参考图。
- 风格增强：内置参考资产可用时是否加载；缺失时继续使用文字视觉 DNA。

只询问会改变检索或输出的缺失项。用户只提供教材、年级册次和单元时，默认使用 `单元复习海报`、竖版、纯生图，并自动检索标题、核心词汇、核心句型、知识提示和配图语义；不默认任何教材版本，不要求用户逐项填写海报字段。

### 2. 初始化任务目录

先把 `{baseDir}` 确定为本 Skill 所在目录，并把 `{python}` 确定为当前系统可用的 Python 3.10+ 命令，再执行：

用户明确要求执行后，使用；可用 `--style-id` 指定内置主题。初始化脚本会检查该主题的案例图是否真实存在，并自动写入参考图模式或文字模式：

```bash
{python} {baseDir}/scripts/init_material_job.py --output <任务目录> --slug <任务名> --units <单元...> --material-type <词汇复习海报或单元复习海报>
```

单张任务只传一个单元；批量任务为每个单元建立独立子目录和清单。不得覆盖现有文件，除非用户明确授权并传入 `--force`。

### 3. 检索并核验教材

按照 `source-policy.md` 和所选资料类型的内容契约，先核验截至当前日期的最新适用版本，再搜索单元标题、词汇、句型、知识提示与必要语境。优先使用当前运行时的原生联网搜索；不可用时使用已安装且能够打开页面正文的搜索工具。检查关键页面正文，不把搜索摘要当作最终证据，不因旧版资料更丰富而默认旧版。

输出“教材确认卡”，至少包含：

- 出版社、教材、版本/年份、版本策略、检索日期、最新版本核验状态、版本差异、年级册次、单元和单元标题。
- 主要来源及其证据级别。
- 冲突、缺口和置信度。

**确认门 A：** 用户明确确认教材身份前，不整理最终词汇清单，不生图。若用户上传清晰教材页，可把它作为主要来源，但仍标明页码或文件位置。

### 4. 整理内容清单

根据确认过的教材身份，先尽量找全可核验候选内容，再按 `content-contracts.md` 确定性选取。词汇按“重要性等级 → 教材顺序 → 英文词形”排序，使用版式能容纳的最大档位 `25 / 20 / 16 / 12 / 9`，对应网格 `5×5 / 5×4 / 4×4 / 4×3 / 3×3`。不足 9 个时停止并补充来源，不得编造。

`单元复习海报` 的核心句型和知识提示各选 2–4 组：候选达到 4 组时固定选 4 组，只有 2–3 组时全部选入，少于 2 组时继续检索。一个问答对计为一个句型组；一个中文学习功能及其 1–2 条英文句式计为一个知识提示组。

把全部候选、入选状态、排序依据和溢出项写进清单。每条入选内容记录成品区域、内容类型、可见文字、配图或呈现方式、使用场景和来源位置；不得丢失候选审计轨迹。

词汇行必须提供具体配图语义。`词汇复习海报` 必须且只能包含核心词汇；`单元复习海报` 必须且只能包含核心词汇、核心句型和知识提示。语言功能类知识提示默认使用“中文学习功能＋英文核心句式”，例如“表达需求＋What would you like ...?＋I’d like ...”。同一功能有两条英文句式时，在清单英文列使用 `<br>` 作为不可见换行标记，并把两条句式分别加入白名单。英文不得写成 `Make requests`、`Ask about preferences` 等标签直译。中文功能和英文句式都加入唯一可见文字白名单。若来源不足，继续检索或请求教材页，不得省略必需模块或以词汇表冒充完整单元海报。

按照 `manifest-schema.md` 完成每份资料的 `material-manifest.md`，并生成唯一可见文字白名单。白名单必须穷举标题、标签、学习内容和页脚；提示词不得出现未列入白名单的可见文案。

按照 `age-adaptation.md` 填写 `学习者年龄段` 和 `年龄适配方式`，并说明人物年龄、画面成熟度与信息密度将如何变化。

使用以下命令从清单生成与最终成品分区一致的确认卡：

```bash
{python} {baseDir}/scripts/check_layout_capacity.py <material-manifest.md>
{python} {baseDir}/scripts/validate_manifest.py <material-manifest.md> --stage confirm
{python} {baseDir}/scripts/render_confirmation_card.py <material-manifest.md>
```

容量预检根据所选主题、版式骨架、词汇数量、句型负载、知识提示行数和最长词给出 `pass`、`review`、`split_required` 或 `override_confirmed`。这些阈值用于提示版式风险，不替代视觉判断：`review` 不阻断；明显超载时选择自动适配、拆页或经用户确认后单页尝试。

**确认门 B：** 向用户展示“最终成品内容确认卡”，包括教材标题、词汇及配图语义、句型及使用场景、知识提示、六个成品分区、页面拆分、风格和年龄适配。只有用户确认这些最终可见内容和区域后，才把 `内容确认` 标记为 `已确认`。不得只展示词汇、音标和中文义后直接生图。

历史清单若缺少 `知识提示语言`，或仍填写 `中英双语`，可以继续读取历史交付记录；但重新确认或重新生图前必须升级为 `中文功能 + 英文核心句式` 并重新通过确认门 B。

### 5. 生图前校验

运行：

```bash
{python} {baseDir}/scripts/validate_manifest.py <material-manifest.md> --stage generate
```

批量任务运行：

```bash
{python} {baseDir}/scripts/validate_batch.py <任务目录> --stage generate
```

校验通过后，为每张图片生成只读标准任务包：

```bash
{python} {baseDir}/scripts/build_generation_package.py <material-manifest.md>
```

生成任务包后、调用图片工具前，运行首张就绪度预检；低于 90 分或任一硬项未通过时先修正任务包，不得把可预防冲突留给图片生成后重试：

```bash
{python} {baseDir}/scripts/generation_preflight.py <generation-package.md> --min-score 90
```

预检必须比较任务包记录的清单文件指纹与当前 `material-manifest.md`；清单在任务包生成后发生任何变化，均视为旧任务包并阻断生图。任务包不与清单放在同一目录时，使用 `--manifest <material-manifest.md>` 明确来源。

预检必须解析唯一运行时像素尺寸、按资料类型覆盖冲突的风格模块、为可能携带文字或数字的配图加入空白抽象约束，并锁定学习内容的出现次数。生成包必须把“允许出现的白名单”和“实际渲染计划”分开：标题区、单元主题区、主体模块标签和学习内容分别绑定区域与次数。若白名单同时包含拆分标题及其拼接别名，只渲染成品分区确认中的拆分形式，并从整个任务包移除拼接别名；其他没有区域归属的白名单文字以 `P01` 阻断生成。

安装或复制到另一台电脑后，先验证随 Skill 打包的参考资产：

```bash
{python} {baseDir}/scripts/verify_reference_assets.py
```

生图 Agent 和批量子 Agent 只读取 `generation-package.md`。其中的 `EXACT MODULES` 是完整主体模块白名单，`FORBIDDEN MODULES` 是硬禁止清单；任何额外模块、栏目或学习面板均视为失败。核心风格锚点、白名单和知识提示内容保持不变；精确区域比例、列数和装饰组合按任务包中的自适应版式说明处理。

校验失败时修复清单或重新请求确认；不得绕过确认字段。

### 6. 生成图片

使用当前运行时可用的原生图片生成工具制作完整位图；支持用途分类时选择 `scientific-educational`。在 Codex 中优先使用内置 `imagegen` 路径。把用户上传图片标注为“风格参考”，除非用户明确要求编辑原图。

- 使用清单生成的 `generation-package.md` 作为完整提示词任务包，逐字引用其中的唯一可见文字和固定风格块。
- 提示词必须按 `illustration-semantics.md` 分成三段：`VISIBLE TEXT — RENDER VERBATIM`、`NON-VISIBLE VISUAL INSTRUCTIONS — NEVER RENDER AS TEXT` 和 `FORBIDDEN VISIBLE TEXT`。不得把 `salad bowl`、`tea cup` 等裸配图短语与白名单文字混排。
- 逐区对应清单中的成品分区；不得在生图时临时增加未确认的句型、口号、栏目名或页脚文字。
- 明确传入学习者年龄段、人物视觉年龄、场景成熟度、色彩装饰成熟度和信息密度。
- 任务包中的独立年龄锁定块优先于风格协议或参考图中冲突的人物年龄线索，但只覆盖人物、场景、道具和装饰成熟度；不得因此替换用户确认的版式、媒介、主色或结构。小学高年级及以上必须明确排除幼儿头身比、婴儿脸、超大头眼、Q版身体和幼儿园式场景。
- 生图前按以下顺序判断风格输入，不得反转优先级：①用户本轮提供且可读取的参考图；②所选主题在清单中登记且真实存在的内置案例图；③对应风格协议的完整文字视觉 DNA。
- 命中①或②且运行时支持图片输入时，必须把图片标为“风格与版式参考”传入，默认借鉴其栏目比例、网格结构、配色层级、信息密度和装饰节奏；仍须替换全部教材文字、人物和主题插图。只有图片缺失、不可读或运行时不支持图片输入时，才使用③。
- 回退到③时，把 `风格生成方式` 记录为 `文字视觉 DNA`、`风格参考图` 记录为 `无`，继续生成，不得中止或要求用户补图。
- 对任一内置风格，原样加入对应协议中的核心风格提示块；把比例范围、典型道具和人物数量作为强默认值，根据内容负载与参考图适配，不退化为宽泛风格词。
- 区分“单元格尺寸一致”和“内容强制铺满”：允许插图留白，必须保持人物、物体、图标、边框和文字的自然宽高比；不得拉伸、压扁或使用压缩/扩展字体。
- 对带外框的内容区明确边框数量、标题标签与外框的连接方式、内部线型，防止模型自行增加嵌套框或重复顶边。
- 不设置跨主题统一的头部或底部比例。优先遵循所选风格的版式指纹，同时允许为可读性调整精确高度、列数、人物数量和底部组合；媒介、主色、标题层级、边框拓扑与年龄气质保持稳定。
- 六类成品分区用于确认内容责任，不强制形成六个独立视觉框。相关区域可合并呈现，但每类已确认内容仍需清晰可识别并接受逐区验收。
- 命中参考图时，把参考图当作版式蓝图而不只是配色参考；没有参考图时，使用对应协议中同一套版式指纹，尽量保持头部、内容区和底部的构图一致。
- 把每条配图语义单独标记为 `NON-VISIBLE VISUAL INSTRUCTION`，并明确声明“配图说明只用于决定画什么，绝不能作为标签、注释或正文显示”；词卡内只允许出现编号、英文词汇和中文释义。
- 对所有插图统一锁定潜在文字表面：衣服、球衣、帽子、书包、书本封面、纸张、屏幕、黑板、标牌、机器人面板、包装、奖牌等默认使用空白或抽象图案；除非白名单把字符明确分配给该内容项，否则不得出现字母、单词、数字、品牌、徽标或口号。
- 同页出现 `drink/thirsty`、`healthy/delicious`、`favourite/dear` 等易混词时，使用 `illustration-semantics.md` 的不同动作或状态证据，避免只换人物或道具颜色。
- 知识提示使用“中文学习功能＋英文核心句式”的上下结构；同一功能可放一至两条可迁移句式。英文较长时自然换行或扩展提示区，不得压缩字宽或缩成不可读小字，不显示学习功能的英文标签直译。
- 明确要求不增加文字、不重复内容、不改变翻译、配图与词义一一对应。
- 单张任务首次只生成一张，并把第一张作为预期成品；不得主动生成草稿、备选方案或风格变体。只有首张出现已记录的硬验收错误时才允许一次定向重试，第三次生成必须取得用户明确同意。批量任务按清单逐张调用，不用一次调用代替不同海报。
- 运行时支持子 Agent 且批量任务有至少2张相互独立的图片时，按照 `multi-agent-batch.md` 由主 Agent 冻结清单、提示词公共块和文件名，再把每张图分配给独立子 Agent。子 Agent 不得自行检索教材、修改白名单或覆盖其他任务文件。
- 同一批次先按照 `illustration-semantics.md` 冻结配图语义。同一个词在不同主题中保持核心对象或动作一致，只改变画风、构图和局部道具。
- 将项目交付图片保存到对应任务目录，采用稳定文件名且不覆盖已通过版本。
- 默认保持纯生图。只有用户主动选择，或连续重试仍无法达标且用户再次确认时，才切换其他排版方式。

### 7. 验收与重试

按照 `qa-checklist.md` 检查每张图片。发现问题时记录具体差异，并只重做失败图片：

- 每张图片复制到任务目录后，先运行 `scripts/validate_output_images.py` 检查文件格式、像素尺寸、宽高比和批量数量；尺寸失败不得通过拉伸修复，只能重新生成或经用户确认后等比裁切。
- 尺寸通过后，使用当前运行时的原生视觉能力打开原始分辨率图片，按标题区、词汇区、句型区和知识提示区逐区对照唯一可见文字白名单。检查漏字、多字、错字、字母数字粘连、重复编号和配图说明误印，并只重做确认失败的图片。

- 第一次重试：针对单一错误强化约束。
- 第二次重试：降低单页信息密度或拆页；如改变页数或文字，回到确认门 B。
- 两次仍失败：如实报告，询问是否继续纯生图或改用可选文字准确模式。

不得把“图片已生成”视为“图片已验收”。批量任务必须同时检查单页和整组一致性。

### 8. 交付

交付时提供：

- 最终图片的完整路径。
- 对应的 `material-manifest.md`。
- 来源记录和教材确认状态。
- 验收结果、已知限制及重试记录。
- 使用的生成模式；默认写明“内置图片生成，纯生图”。
- 批量任务使用的执行方式：多子 Agent 并行或顺序回退，并记录实际并发数。

不要自动发布、上传或同步到外部平台。

## 完成标准

只有同时满足以下条件才报告完成：

- 两个确认门均通过。
- 清单校验通过。
- 请求的图片数量齐全。
- 所有可见文字与白名单一致。
- 主体模块与 `EXACT MODULES` 完全一致，数量稳定且没有额外学习模块。
- 单词、释义、句型和插图语义匹配。
- 人物年龄、场景、色彩装饰和信息密度符合已确认的学习者年龄段。
- 文件已保存到用户可访问的目标目录。
